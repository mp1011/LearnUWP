<UserControl
    x:Class="LearnUWP.Views.Field"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LearnUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <StackPanel Orientation="Horizontal" Style="{x:Bind FieldStyle, Mode=OneWay}">
            <TextBlock Text="{x:Bind Label}"/>
            <ContentControl Content="{x:Bind FieldContent, Mode=OneTime}" />
            <TextBlock Style="{StaticResource ValidationMessage}" Text="{x:Bind ErrorText, Mode=OneWay}"  />
        </StackPanel>
    </Grid>
</UserControl>
